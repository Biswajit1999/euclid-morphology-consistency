# Claims Ledger

| # | Claim | Basis | Status |
|---|---|---|---|
| 1 | The Q1 morphology catalog's disk+bulge two-component Sersic fit is unpopulated (NULL) for all 29,953,430 rows in the full table. | Live `COUNT(*)` query via `audit-schema`, 2026-08-29, no WHERE-clause selection bias | Verified |
| 2 | On 20,000 real, quality-flagged Q1 objects, VIS-vs-NIR Sersic index shows a statistically significant (95% bootstrap CI [-0.00317, -0.00078], excludes zero; Wilcoxon p=2.9e-13) but practically negligible (~460x smaller than the MAD=0.731 per-object scatter) population-level median bias of -0.00158, and weak per-object correlation (Pearson r=0.198). | `results/default/summary.json`, generated by `euclidmorph run --n 20000`, not hand-typed | Verified: generated 2026-08-29 |
| 3 | Sersic index (VIS) and CAS concentration show a moderate positive correlation (Spearman r=0.428, p~0). | `results/default/summary.json` | Verified: generated 2026-08-29 |
| 4 | Binning `position_dependent_residuals` within each of the three deep fields' own footprint (rather than the combined ra/dec range of all three) gives 19 of up to 108 possible bins with >=20 objects, versus 3/36 for the combined-range binning it replaced. | `results/default/position_residuals.csv`, generated by `euclidmorph run --n 20000` | Verified: generated 2026-08-29 |
| 5 | This project makes no claim about which of two disagreeing measurements is more accurate. | docs/METHODS.md, docs/LIMITATIONS.md | True by construction |
| 6 | This project does not duplicate `euclid-star-shapes` or `euclid-spectra-quality`; it covers a distinct research question. | docs/DATA_SOURCES.md | Reasoned claim, based on direct comparison of research questions |
| 7 | A server-side `ORDER BY object_id` on the live join query is ~30x slower than the same query unordered (measured: ~235s vs ~8s for one field's `TOP 70`). | Direct timing against the live IRSA TAP service, 2026-08-29 | Verified |

## Superseded / corrected claims

| # | Original claim (v0.1.0) | Why it was wrong | Correction |
|---|---|---|---|
| S1 | "VIS-vs-NIR Sersic index shows no population-level bias (median diff ~0)." | Reported only a point estimate, with no confidence interval or significance test -- an external review (Codex) flagged this as stronger than a point estimate alone supports. The bias is in fact statistically significant (CI excludes zero, Wilcoxon p=2.9e-13), just practically tiny relative to per-object scatter. | Claim 2 above: report the CI, the significance test, and the effect size relative to MAD together, not "no bias" alone. |
| S2 | "The default sample is spatially concentrated (only 3/36 position bins usable)." | This was true of the v0.1.0 sample (drawn via a single unordered, spatially-unconstrained `TOP N` query that happened to draw almost entirely from one field), but the underlying cause -- non-stratified sampling -- was fixed in v0.2.0, not merely documented as a limitation. | v0.2.0 samples each deep field separately (`src/euclidmorph/data.py`); claim 4 above reports the resulting 19/108 usable bins for the *new* per-field binning, which is not directly comparable to the old 3/36 combined-range figure. |

## Open / contested claims

- Whether the larger median residual seen in a couple of the smallest
  (n~20-30) position bins reflects a real localized effect or ordinary
  small-sample scatter is not resolved (`docs/LIMITATIONS.md`,
  `docs/METHODS.md`).
