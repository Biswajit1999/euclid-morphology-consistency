# Security Policy

This package's only network access is a single, fixed, documented HTTPS
query of the public IRSA TAP endpoint (`src/euclidmorph/data.py`), no
authentication required. Open a GitHub Issue labeled `security` or use
GitHub's private vulnerability reporting for anything found.
